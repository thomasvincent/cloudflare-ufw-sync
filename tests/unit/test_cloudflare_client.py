"""Lightweight placeholder retained for backwards-compatibility.

The real functional tests live in tests/unit/test_cloudflare.py. We keep this
file around (with a passing test) so that any scripts or docs referencing it
don't break while we gradually expand coverage elsewhere.
"""


def test_smoke():
    assert True
